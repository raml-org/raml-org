---
---
[
   {% for testimonial in site.testimonials %}
     {
        "author"    : "{{ testimonial.author | remove: '<p>' | remove: '</p>' | strip_new_lines }}",
        "location"     : "{{ testimonial.location | remove: '<p>' | remove: '</p>' | strip_new_lines }}",
        "content"  : "{{ testimonial.content | remove: '<p>' | remove: '</p>' | strip_new_lines }}"
     }
   {% endfor %}
]